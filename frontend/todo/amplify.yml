version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/train
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/train/build
    files:
      - "**/*"
  cache:
    paths:
      - frontend/train/node_modules/**
