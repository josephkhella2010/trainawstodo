version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/todo
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/todo/build
    files:
      - "**/*"
  cache:
    paths:
      - frontend/train/node_modules/**
